import * as React from 'react';

import './$CMP_FILE$-cmp.scss';

type $CMP$CmpProps = {};

const $CMP$Cmp: React.ComponentType<$CMP$CmpProps> = ( { ...props } ) => (
    <div>Hello from $CMP$Cmp</div>
);

export { $CMP$CmpProps, $CMP$Cmp };
